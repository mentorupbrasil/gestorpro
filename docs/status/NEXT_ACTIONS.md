# Próximas ações

1. Executar checklists manuais P0.1–P0.3 em `docs/implementation/` com MFA ativo (`/app/security`).
2. Validar fluxo completo: triagem → consulta → conclusão no encounter demo `e8000000-0000-4000-8000-000000000001`.
3. Gerar tipos oficiais (`npm run types:supabase:generate`) e testes negativos de bypass no PostgreSQL.
4. Não iniciar a Fase B enquanto qualquer gate da Fase A permanecer aberto.
