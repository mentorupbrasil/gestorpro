# Próximas ações

1. Aplicar no Supabase: `supabase/migrations/202607140019_fase_j_snapshot_billing_payment.sql`
2. Commitar feature quando quiser
3. Próximo arrocho: Fase K (portais) → L → M → N (N = NO-GO humano)
4. Sem merge/`main`/produção
