# Próximas ações

1. Gerar tipos oficiais com `SUPABASE_ACCESS_TOKEN` + `npm run types:supabase:generate` (hoje só offline).
2. Avaliar constraints compostas multi-tenant em banco descartável.
3. CI/PR remoto e governança do repositório público (ação humana).
4. Não iniciar a Fase B enquanto typegen oficial e demais gates restantes da Fase A permanecerem abertos.
