# Próximas ações

1. Confirmar apply de `202607140012` no Supabase (SQL Editor) se ainda não rodou.
2. Commitar nesta feature (não na `main`) as correções de auth/higiene quando o dono pedir.
3. Ver checks da PR #11 no GitHub (CI/CodeQL/Dependency Review) — status local ≠ status remoto.
4. Ações humanas: `docs/status/HUMAN_ACTIONS.md` (privado, token, Vercel, branch protection).
5. Sem merge/`main`/produção.
