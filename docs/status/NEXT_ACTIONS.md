# Próximas ações

1. Commitar (quando o dono quiser) as correções de auth unit-scoped em exames/check-in/display.
2. Confirmar apply da `012` no Supabase se ainda não confirmado.
3. Checks remotos PR #11 no GitHub (não afirmar verde sem evidência).
4. Ações humanas: `docs/status/HUMAN_ACTIONS.md`.
5. Residual Fase A depois: RLS policies de exames ainda baseadas em permissão tenant-wide (lista pode vir vazia para unit-only via RLS) — migrar policies com `has_encounter_permission`/`has_unit_permission` quando for o próximo bloco de banco.
6. Sem merge/`main`/produção.
