# Próximas ações

1. Commitar + push (tipos Zod + lint integrações) para revalidar Vercel/`quality`
2. Humano: em Settings → Code security, ativar **Dependency graph** (e Dependency review se aparecer) — senão o check `review` continua vermelho
3. Aplicar `202607140024_stabilization_occupational_structure_pcmso_rpc.sql` (arquivo `.sql` puro no SQL Editor — não colar diff do git)
4. Próxima dívida útil (escolher uma): preço financeiro server-side **ou** ASO PDF/storage **ou** portal invite/IDOR
5. Sem merge/`main`/produção — **NO-GO**
