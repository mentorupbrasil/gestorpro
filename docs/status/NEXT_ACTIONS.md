# Próximas ações

1. Push do typegen offline (fingerprint) — desbloqueia `types:supabase:check` no CI
2. Confirmar checks da PR #11 após o push
3. Aplicar `202607140024_...sql` no Supabase se ainda não aplicou (`.sql` puro)
4. Próxima dívida útil: preço server-side **ou** ASO PDF/storage **ou** portal invite/IDOR
5. Sem merge/`main`/produção — **NO-GO**
